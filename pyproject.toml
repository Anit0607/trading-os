[project]
name = "trading-os"
version = "0.1.0"
description = "Read-only Vercel cloud dashboard for the local Trading OS worker."
requires-python = ">=3.11"
dependencies = [
  "psycopg[binary]>=3.2.0",
]

[tool.vercel]
entrypoint = "app.main:application"
